#include "TwoPlayerDijkstra.h"

/*
double TwoPlayerDijkstra<xyLoc, tDirection, MapEnvironment>::GCost(const  xyLoc &s1, const xyLoc &s2 ) {
//	if( abs(s1.x-s2.x)==1 && abs(s1.y-s2.y)==1 ) return 1.5;
	return 1.;
}
*/
